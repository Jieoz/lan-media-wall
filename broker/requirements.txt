# LAN Media Wall — broker runtime dependencies.
# Python 3.11+ recommended (code runs on 3.10+).
websockets>=12.0
PyYAML>=6.0

# Optional: renders a scannable terminal QR for the lmw:// pairing URI (§15).
# Without it the broker still prints the URI as text. Uncomment to enable:
# qrcode>=7.0

# Dev/test only:
pytest>=7.0
pytest-asyncio>=0.21
